---
type: index
area: drawings
---

# 🎨 绘图

Excalidraw 新建的图会自动保存在这里，素材库、脚本和中文字体也集中在本目录下。

```dataview
TABLE WITHOUT ID file.link AS "绘图", dateformat(file.mtime, "yyyy-MM-dd HH:mm") AS "最近修改"
FROM "40-资源/绘图"
WHERE file.ext = "excalidraw" OR contains(file.tags, "excalidraw")
SORT file.mtime DESC
```
