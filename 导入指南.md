---
type: guide
tags:
  - import
---

# 常用导入指南

## Markdown / 文本文件

1. 直接把 `.md`、`.txt` 文件拖入 `00-收件箱`。
2. 或在命令面板运行“Markdown 格式转换器”，统一常见 Markdown 差异。
3. 图片和其他附件统一放到 `90-附件`。

## Notion、Evernote、Apple Notes、OneNote

在“设置 → 第三方插件 → 浏览”中安装 Obsidian 官方 **Importer**，然后从左侧功能区或命令面板运行导入。导入目标建议先选 `00-收件箱`，确认链接、附件和日期无误后再分类。

## 网页内容

推荐使用 Obsidian Web Clipper，默认保存到 `00-收件箱`。导入后补充来源链接、作者和主题标签。

## 通用整理规则

- 新内容先进入收件箱，不在导入时过度分类。
- 附件集中放在 `90-附件`，避免散落。
- 保留原始来源 URL；长内容先写三行摘要，再决定是否精读。
- 每周至少清空一次收件箱。
